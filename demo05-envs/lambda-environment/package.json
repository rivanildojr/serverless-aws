{
  "name": "lambda-environment",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "invoke-local": "sls invoke local -f commit-message-scheduler --logger",
    "invoke-prod": "sls invoke local --stage prod -f commit-message-scheduler --logger"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.804.0",
    "axios": "^0.21.0",
    "cheerio": "^1.0.0-rc.3",
    "env-var": "^7.0.0"
  }
}
